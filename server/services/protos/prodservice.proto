syntax = "proto3";

package protos;

import "message.proto";


message ProdsRequest {
  int32  Size=1;
}

message ProdsResponse {
  repeated ProdModel data=1;
}

service ProdService {
  rpc GetProdsList(ProdsRequest) returns(ProdsResponse);
}